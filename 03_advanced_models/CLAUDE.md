# CLAUDE.md — Stage 03: Advanced Models

Inherits root. ONE job: train Random Forest + XGBoost through the SAME shared
pipeline as Stage 02 (fair comparison).

- Cannot start until GATE A exists.
- GATE C: metrics.json + per-model .pkl present.
