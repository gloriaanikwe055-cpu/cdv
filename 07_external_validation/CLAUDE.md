# CLAUDE.md — Stage 07: External Validation

Inherits root. ONE job: score the frozen best model against a REAL public dataset
it never trained on. Real data is READ-ONLY (R8).

- Cannot start until GATE D exists.
- A performance drop is EXPECTED and reported honestly — it is the headline
  finding (CLAUDE.md Sec 4). Never fabricate to close the gap (R1).
- If no real dataset is provided, record SKIPPED honestly; invent nothing.
